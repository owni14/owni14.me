import Loader from "./_components/ui/loader/Loader";

/**
 * Loading UI used all of pages
 * @returns {JSX.Element} JSX element
 */
const Loading = (): JSX.Element => {
  return <Loader />;
};

export default Loading;
